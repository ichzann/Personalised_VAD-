# Phase 0 core dependencies (Apple Silicon / CPU only).
torch
torchaudio
numpy
scipy
soundfile
matplotlib
tqdm
python-dotenv

# Phase 2 frozen backbone (decided in Phase 2, ROADMAP §7): SpeechBrain ECAPA-TDNN
# speaker embeddings + Silero VAD. Both run frozen on CPU; no HF token needed.
speechbrain
silero-vad

# Phase 6/7 live demo: microphone capture for enrollment + streaming detection.
sounddevice
